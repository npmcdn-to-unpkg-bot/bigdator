#!/bin/bash

# run inside the root folder: . ./server/data/database.sh (note the extra dot!)
# you might need to run chmod+x ./server/data/database.sh on beforehand

export PGHOST=your_pg_host
export PGUSER=your_pg_user
export PGPASSWORD=your_pg_password
export PGDATABASE=your_pg_database

